# 03 语义地雷（上）：指针 / 数组 / 内存

对应文章：《C语言语义地雷：'语义刺客'的硬件谋杀实录①》

## 文件
- `semantic_traps_1.c` —— 5 个语义陷阱及安全写法。

## 构建与运行
```bash
make run
```

## 覆盖的陷阱
1. 字符串字面量只读（用字符数组才能改）
2. 野指针（诞生即绑定内存）
3. 数组退化为指针（函数参数显式传长度）
4. `memset(p,0,sizeof(p))` 只清指针大小 → 用 `sizeof(类型)`
5. 非空指针 ≠ 空字符串；`malloc` 必检查
