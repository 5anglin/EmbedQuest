# 04 语义地雷（下）：边界 / 求值顺序 / 溢出

对应文章：《C语言语义地雷：'语义刺客'的硬件谋杀实录②》

## 文件
- `semantic_traps_2.c` —— 陷阱 6~10 及安全写法。

## 构建与运行
```bash
make run
```

## 覆盖的陷阱
6. 差一错误（用 `<` 与 `ARRAY_SIZE`）
7. 同一表达式内读写同一变量（求值顺序未定义）
8. 短路求值：先判空再解引用
9. 整数溢出：先用更宽类型再检查范围
10. `main` 必须有明确 `return`
