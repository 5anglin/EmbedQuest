# 06 预处理惊魂记：你的宏正在肢解单片机

对应文章：《C语言预处理惊魂记：你的宏正在肢解单片机》

## 文件
- `macro_pitfalls.c` —— 宏的 4 类陷阱与安全写法（含 GCC 语句表达式 `MAX`）。

## 构建与运行
```bash
make run
```

## 要点
- 参数与整体都要加括号：`#define SQUARE(x) ((x)*(x))`
- 多语句宏用 `do { ... } while(0)` 封装，避免悬空 else
- 指针类型用 `typedef` 而非宏定义
