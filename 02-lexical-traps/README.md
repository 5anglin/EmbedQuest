# 02 词法暗礁：5大死亡陷阱

对应文章：《C语言词法暗礁：嵌入式开发者必须绕过的5大死亡陷阱》

## 文件
- `lexical_traps.c` —— 5 个陷阱的"错误说明 + 正确可运行版本"。

## 构建与运行
```bash
make run
```

## 覆盖的陷阱
1. `=` 与 `==`（Yoda 写法：常量左置）
2. 位运算 `&` 误当逻辑 `&&`
3. 相邻运算符的"最大吞噬"解析（用空格分隔）
4. 前导 `0` 是八进制字面量
5. 多字符字符常量 vs 字符串字面量
